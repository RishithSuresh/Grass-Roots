# GrassRoots backend environment variables
# Copy/edit values below. Do NOT commit real secrets to version control.

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_NAME=grassroots_db
DB_USER=root
DB_PASS=RishithRockz10

# Server port
PORT=3000

# Node environment (development|production)
NODE_ENV=development

# JWT secret used when JWT auth added (change before production)
JWT_SECRET=change_this_jwt_secret

# Log level (optional)
LOG_LEVEL=info
