{
  "name": "farmer-voice-bot-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for Farmer Voice Bot",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network ganache",
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.14.0",
    "dotenv": "^16.0.3"
  }
}
